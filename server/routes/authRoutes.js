import {Router} from 'express';import bcrypt from 'bcryptjs';import {q} from '../db.js';import {auth,signUser} from '../auth.js';const r=Router();r.post('/auth/login',async(req,res)=>{const {email,password}=req.body;const u=(await q('SELECT * FROM users WHERE lower(email)=lower($1) AND active=true',[email||''])).rows[0];if(!u||!await bcrypt.compare(password||'',u.password_hash))return res.status(401).json({error:'Invalid email or password'});res.json({token:signUser(u),user:{id:u.id,name:u.name,email:u.email,role:u.role}})});r.get('/me',auth,(req,res)=>res.json(req.user));export default r;
