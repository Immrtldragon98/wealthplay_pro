import {Router} from 'express';import multer from 'multer';import {q} from '../db.js';import {auth,allow} from '../auth.js';import {parseMasterExcel,parseSapStatusExcel} from '../excel.js';import {findOrCreateLocation,getDepartment} from '../services/locationService.js';import {audit} from '../services/auditService.js';
const r=Router();const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:15*1024*1024}});
async function scope(req){const department_code=(req.body.department_code||'').trim();if(!department_code)throw new Error('Select a department before importing');const dept=await getDepartment(department_code);if(!dept)throw new Error('Unknown department');return {department_code,dept,area:req.body.area||''}}
r.post('/import/master/preview',auth,allow('planner','admin'),upload.single('file'),async(req,res)=>{if(!req.file)return res.status(400).json({error:'Excel file required'});const {department_code,area}=await scope(req);if(!area)return res.status(400).json({error:'Select an Area before importing a spare master'});const out=parseMasterExcel(req.file.buffer,area,department_code),seen=new Set(),dups=[];out.materials.forEach(m=>{const k=`${m.material_code}|${department_code}|${m.area}|${m.equipment||''}|${m.sub_equipment||''}`;if(seen.has(k))dups.push(k);seen.add(k)});const unmapped=[...new Set(out.materials.filter(x=>!x.sap_location_code).map(x=>`${x.area} → ${x.equipment||'(Area level)'}${x.sub_equipment?' → '+x.sub_equipment:''}`))];res.json({fileName:req.file.originalname,totalRows:out.materials.length,materials:out.materials.slice(0,100),issues:out.issues,duplicateUsages:dups.slice(0,50),unmappedLocations:unmapped,message:'Unmapped equipment can be imported and mapped to SAP later.'})});
r.post('/import/master/confirm',auth,allow('planner','admin'),upload.single('file'),async(req,res)=>{if(!req.file)return res.status(400).json({error:'Excel file required'});const {department_code,area}=await scope(req);if(!area)return res.status(400).json({error:'Select an Area before importing a spare master'});const out=parseMasterExcel(req.file.buffer,area,department_code);let added=0,updated=0,skipped=0;for(const row of out.materials){const x={...row,department_code};let m=x.material_code?(await q('SELECT * FROM materials WHERE upper(material_code)=upper($1)',[x.material_code])).rows[0]:null;if(!m){m=(await q(`INSERT INTO materials(material_code,description,part_number,uom,manufacturer,vendor,created_by,updated_by) VALUES($1,$2,$3,$4,$5,$6,$7,$7) RETURNING *`,[x.material_code,x.description,x.part_number,x.uom,x.manufacturer,x.vendor,req.user.id])).rows[0];added++;}else{const merged={...m};for(const k of ['description','part_number','uom','manufacturer','vendor'])if(x[k]!==null&&x[k]!=='')merged[k]=x[k];m=(await q(`UPDATE materials SET description=$1,part_number=$2,uom=$3,manufacturer=$4,vendor=$5,active=true,updated_by=$6,updated_at=NOW() WHERE id=$7 RETURNING *`,[merged.description,merged.part_number,merged.uom,merged.manufacturer,merged.vendor,req.user.id,m.id])).rows[0];updated++;}const loc=await findOrCreateLocation(x);await q(`INSERT INTO material_usages(material_id,location_id,required_qty,notes,created_by,updated_by) VALUES($1,$2,$3,$4,$5,$5) ON CONFLICT(material_id,location_id) DO UPDATE SET required_qty=COALESCE(EXCLUDED.required_qty,material_usages.required_qty),notes=COALESCE(NULLIF(EXCLUDED.notes,''),material_usages.notes),active=true,updated_by=$5,updated_at=NOW()`,[m.id,loc.id,x.required_qty,x.notes,req.user.id])}await q(`INSERT INTO import_history(import_type,file_name,total_rows,added_rows,updated_rows,skipped_rows,issue_rows,details,imported_by) VALUES('master',$1,$2,$3,$4,$5,$6,$7,$8)`,[req.file.originalname,out.materials.length,added,updated,skipped,out.issues.length,JSON.stringify({department_code,area,issues:out.issues}),req.user.id]);res.json({ok:true,total:out.materials.length,added,updated,skipped,issues:out.issues})});
r.post('/import/sap/preview',auth,allow('planner','admin'),upload.single('file'),async(req,res)=>{if(!req.file)return res.status(400).json({error:'SAP Excel export required'});const {department_code}=await scope(req),out=parseSapStatusExcel(req.file.buffer);const outside=out.rows.filter(x=>x.sap_location_code&&!x.sap_location_code.startsWith(department_code)).map(x=>x.material_code);res.json({fileName:req.file.originalname,totalRows:out.rows.length,rows:out.rows.slice(0,100),issues:out.issues,outsideDepartment:outside.slice(0,50),message:`Material Code updates Store/PR/PO. SAP locations are validated against ${department_code}.`})});
r.post('/import/sap/confirm',auth,allow('planner','admin'),upload.single('file'),async(req,res)=>{if(!req.file)return res.status(400).json({error:'SAP Excel export required'});const {department_code}=await scope(req),out=parseSapStatusExcel(req.file.buffer);let updated=0,skipped=0;const missing=[],outside=[];for(const s of out.rows){if(s.sap_location_code&&!s.sap_location_code.startsWith(department_code)){outside.push(s.material_code);skipped++;continue}const ex=(await q('SELECT * FROM materials WHERE material_code=$1',[s.material_code])).rows[0];if(!ex){missing.push(s.material_code);skipped++;continue}const store=s.store_qty===null?ex.store_qty:s.store_qty,pr=s.pr_qty===null?ex.pr_qty:s.pr_qty,po=s.po_qty===null?ex.po_qty:s.po_qty;const y=await q('UPDATE materials SET store_qty=$1,pr_qty=$2,po_qty=$3,updated_by=$4,updated_at=NOW() WHERE id=$5 RETURNING *',[store,pr,po,req.user.id,ex.id]);await audit(req.user,'sap_status_update','material',ex.id,s.material_code,ex,y.rows[0]);updated++}await q(`INSERT INTO import_history(import_type,file_name,total_rows,updated_rows,skipped_rows,issue_rows,details,imported_by) VALUES('sap_status',$1,$2,$3,$4,$5,$6,$7)`,[req.file.originalname,out.rows.length,updated,skipped,out.issues.length,JSON.stringify({department_code,missing_material_codes:missing,outside_department:outside,issues:out.issues}),req.user.id]);res.json({ok:true,total:out.rows.length,updated,skipped,missingMaterialCodes:missing,outsideDepartment:outside})});
r.get('/import-history',auth,allow('planner','admin'),async(req,res)=>res.json((await q(`SELECT h.*,u.name imported_by_name FROM import_history h LEFT JOIN users u ON u.id=h.imported_by ORDER BY imported_at DESC LIMIT 100`)).rows));
export default r;
