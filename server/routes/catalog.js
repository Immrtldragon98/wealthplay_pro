import { Router } from 'express';import { q } from '../db.js';import { auth } from '../auth.js';
const r=Router();
r.get('/departments',auth,async(req,res)=>{const x=await q(`SELECT d.*,array_remove(array_agg(a.area_name ORDER BY a.area_name),NULL) areas FROM departments d LEFT JOIN areas a ON a.department_id=d.id AND a.active=true WHERE d.active=true GROUP BY d.id ORDER BY d.department_name`);res.json(x.rows)});
r.get('/options',auth,async(req,res)=>{const {department_code='',area='',equipment=''}=req.query;const p=[],w=['l.active=true'];if(department_code){p.push(department_code);w.push(`l.department_code=$${p.length}`)}if(area){p.push(area);w.push(`l.area_name=$${p.length}`)}if(equipment){p.push(equipment);w.push(`l.equipment_name=$${p.length}`)}const rows=(await q(`SELECT DISTINCT l.area_name,l.equipment_name,l.sub_equipment_name FROM locations l WHERE ${w.join(' AND ')}`,p)).rows;const departments=(await q(`SELECT department_code,department_name,plant_code FROM departments WHERE active=true ORDER BY department_name`)).rows;const vendors=(await q(`SELECT DISTINCT vendor FROM materials WHERE active=true AND vendor IS NOT NULL ORDER BY vendor`)).rows.map(x=>x.vendor);const uniq=k=>[...new Set(rows.map(x=>x[k]).filter(Boolean))].sort();res.json({departments,areas:uniq('area_name'),equipment:uniq('equipment_name'),sub_equipment:uniq('sub_equipment_name'),vendors})});
export default r;
