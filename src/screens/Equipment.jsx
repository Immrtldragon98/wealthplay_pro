import React from 'react';
export default function Equipment({materials,setFilters,setTab}){const groups=Object.values(materials.reduce((a,m)=>{const k=m.equipment||'(Area level)';a[k]??={name:k,count:0,sap:m.sap_location_code};a[k].count++;return a},{})).sort((a,b)=>b.count-a.count);return <><div className="pageTitle"><div><h1>Equipment</h1><p>Select equipment to open its spare list.</p></div></div><div className="listCards">{groups.map(g=><button key={g.name} onClick={()=>{setFilters(f=>({...f,equipment:g.name==='(Area level)'?'':g.name,sub_equipment:''}));setTab('Spares')}}><strong>{g.name}</strong><span>{g.count} spare usages</span>{g.sap&&<small>{g.sap}</small>}</button>)}</div></>}
